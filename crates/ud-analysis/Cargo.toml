[package]
name = "ud-analysis"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Analysis passes over loaded binaries: function discovery, type recovery, and signature matching."

[lints]
workspace = true

[dependencies]
ud-core = { version = "0.1.0", path = "../ud-core" }
ud-format-elf = { version = "0.1.0", path = "../ud-format-elf" }
ud-signatures = { version = "0.1.0", path = "../ud-signatures" }
gimli = { version = "0.33", default-features = false, features = ["read", "std"] }
thiserror = "2"
