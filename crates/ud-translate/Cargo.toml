[package]
name = "ud-translate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "The .ud source-language translation engine: binary → .ud source (decompile) and .ud source → binary (compile), in one crate."

[lints]
workspace = true

[dependencies]
ud-core = { version = "0.1.1", path = "../ud-core" }
ud-ir = { version = "0.1.1", path = "../ud-ir" }
ud-ast = { version = "0.1.1", path = "../ud-ast" }
ud-analysis = { version = "0.1.1", path = "../ud-analysis" }
ud-debug = { version = "0.1.1", path = "../ud-debug" }
ud-arch-x86 = { version = "0.1.1", path = "../ud-arch-x86" }
ud-arch-aarch64 = { version = "0.1.1", path = "../ud-arch-aarch64" }
ud-arch-6502 = { version = "0.1.1", path = "../ud-arch-6502" }
ud-format = { version = "0.1.1", path = "../ud-format" }
thiserror = "2"
